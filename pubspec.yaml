name: life_achievements
description: 用照片和真实事件记录时间留下的痕迹
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  exif: ^3.3.0
  photo_manager: ^3.12.0
  photo_manager_image_provider: ^2.2.0
  lunar: ^1.7.8
  supabase_flutter: ^2.8.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
