# 时间痕迹：小白开始指南

这是一个 Flutter 安卓 App。它读取手机相册的照片，按拍摄日期自动归到"人生卡片"里（人生第 N 天、结婚、毕业、工作、农历节日、生日），支持手动整理，每天回顾"往年的今天"，并用连续留痕解锁成就。

**照片永远只留在手机本地，不上传。** 上云的只有你的日期配置和卡片分组（`life_settings` 表）。

## 第一次打开 App

App 不会自带任何人的日期。首页顶部会显示"还没有纪念日期"，点"填写纪念日期"
输入你自己的生日、结婚纪念日等，卡片和里程碑才会出现；不想用的那一项留空即可，
也可以随时点开某一项右上角的 × 清除。

不登录也能填写和使用，配置会存在手机本机；之后登录（或绑定邮箱）时会同步到云端，
换设备就能拉回来。点右上角的调节图标随时可以再来改。

## 你需要准备什么

1. Android Studio（包含 Android SDK 和模拟器）
2. Flutter SDK：安装后把 `flutter\bin` 加入 Windows PATH
3. 用 Android Studio 的 SDK Manager 安装 Android SDK、Platform-Tools 和命令行工具

装好后在 PowerShell 检查：

```powershell
flutter doctor
flutter doctor --android-licenses
```

按 `flutter doctor` 的提示逐项处理，直到 Android 相关项目基本通过。

## 第一次配置 Supabase

1. 打开 https://supabase.com/ 注册并创建项目。
2. 打开项目的 SQL Editor，把本项目的 `supabase_setup.sql` 全部复制进去并执行。
   如果这个 Supabase 项目是从旧版本升级来的，再执行一次 `supabase_cleanup_legacy.sql`
   清理已废弃的表和列（会删数据，执行前看清楚注释）。
3. 打开 Authentication → Providers，开启 Anonymous Sign-Ins。
4. 打开 Project Settings → API，复制 Project URL 和 publishable key。
5. 把项目根目录的 `env.example.json` 复制一份，改名为 `env.json`，填入上一步的两个值：

```json
{
  "SUPABASE_URL": "https://你的项目.supabase.co",
  "SUPABASE_ANON_KEY": "sb_publishable_xxxxxxxx"
}
```

`env.json` 已经被 `.gitignore` 忽略，不会提交到仓库。**不要把真实密钥写进任何会被提交的文件，也不要把 `service_role` 密钥放进 App。**

## 运行

```powershell
flutter pub get
flutter run --dart-define-from-file=env.json
```

要指定设备就先执行 `flutter devices` 看设备名，再加上 `-d 设备名`。

## 打包 APK

```powershell
flutter build apk --release --dart-define-from-file=env.json
```

成功后 APK 在：

```text
build\app\outputs\flutter-apk\app-release.apk
```

发送到安卓手机安装即可，手机可能需要允许"安装未知来源应用"。

注意：`android/app/build.gradle.kts` 的 release 签名目前还用 debug 密钥，自用没问题；要上架或长期分发需要换成自己的签名配置。

## 账号提醒

App 支持邮箱注册，也支持"免邮箱登录"（匿名账号）。你的日期和卡片分组绑定在当前登录的账号上：

- 用匿名账号时，清除 App 数据、卸载 App、换手机都可能找不回配置
- 想安全换设备，就在设置页把匿名账号绑定到邮箱

照片本身在相册里，重装 App 后重新授予相册权限就能再次读到。
