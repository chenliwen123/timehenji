# 统一换行符：仓库内一律存 LF，避免 Windows 上 checkout 出 CRLF
# 造成整文件差异，也消除每次 git add 刷屏的 warning。
* text=auto eol=lf

# Windows 批处理必须是 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ttf binary
*.otf binary
*.jar binary
*.aar binary
*.keystore binary
*.jks binary
